{% extends 'base.html.twig' %}

{% block title %}Gadget{% endblock %}

{% block body %}
    <h1>Gadget</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gadget.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ gadget.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ gadget.description }}</td>
            </tr>
            <tr>
                <th>Intensity</th>
                <td>{{ gadget.intensity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_gadget_index') }}">back to list</a>

    <a href="{{ path('app_admin_gadget_edit', {'id': gadget.id}) }}">edit</a>

    {{ include('admin_gadget/_delete_form.html.twig') }}
{% endblock %}
