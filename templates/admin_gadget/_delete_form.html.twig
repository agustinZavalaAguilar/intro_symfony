<form method="post" action="{{ path('app_admin_gadget_delete', {'id': gadget.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gadget.id) }}">
    <button class="btn">Delete</button>
</form>
