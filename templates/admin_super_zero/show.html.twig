{% extends 'base.html.twig' %}

{% block title %}SuperZero{% endblock %}

{% block body %}
    <h1>SuperZero</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ super_zero.id }}</td>
            </tr>
            <tr>
                <th>SuperZeroName</th>
                <td>{{ super_zero.superZeroName }}</td>
            </tr>
            <tr>
                <th>RealName</th>
                <td>{{ super_zero.realName }}</td>
            </tr>
            <tr>
                <th>SuperSkillName</th>
                <td>{{ super_zero.superSkillName }}</td>
            </tr>
            <tr>
                <th>SuperSkillDescription</th>
                <td>{{ super_zero.superSkillDescription }}</td>
            </tr>
            <tr>
                <th>Weakness</th>
                <td>{{ super_zero.weakness }}</td>
            </tr>
            <tr>
                <th>Power</th>
                <td>{{ super_zero.power }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_super_zero_index') }}">back to list</a>

    <a href="{{ path('app_admin_super_zero_edit', {'id': super_zero.id}) }}">edit</a>

    {{ include('admin_super_zero/_delete_form.html.twig') }}
{% endblock %}
